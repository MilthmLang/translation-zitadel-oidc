# Language Mapping for zitadel/oidc

This repository provides internationalized error message mappings for `zitadel/oidc`, matching original error codes with localized descriptions so applications can display user-friendly error text in multiple languages.
